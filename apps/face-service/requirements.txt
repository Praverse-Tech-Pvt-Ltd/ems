# v4 - facenet-pytorch: pure Python, no C++ compilation
# Python 3.13-compatible pins
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
numpy>=1.24
opencv-python-headless==4.10.0.84
# torch + facenet-pytorch installed separately — see README step 5
torch==2.6.0+cpu
torchvision==0.21.0+cpu
# facenet-pytorch runtime deps (installed via --no-deps so listed explicitly)
requests>=2.0.0,<3.0.0
tqdm>=4.0.0,<5.0.0
# numpy 2.x has breaking API changes incompatible with facenet-pytorch
numpy>=1.24,<2.0
psycopg2-binary>=2.9.9
pgvector==0.3.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
Pillow==10.2.0
