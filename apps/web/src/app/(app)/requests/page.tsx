import { StubPage } from '@/components/ui/StubPage';

export default function RequestsPage() {
  return (
    <StubPage
      title="Requests"
      icon="send"
      description="Submit and track employee requests — WFH, advance payments, document requests, asset requests, travel approvals, and attendance corrections."
    />
  );
}
